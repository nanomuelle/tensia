# ──────────────────────────────────────────────────────────
# Autenticación Google — Google Identity Services (BK-40)
# ──────────────────────────────────────────────────────────

# Client ID de Google para el flujo OAuth 2.0 Authorization Code + PKCE.
# Se obtiene en https://console.cloud.google.com/ → Credenciales.
# En Vite el valor queda disponible como import.meta.env.VITE_GOOGLE_CLIENT_ID.
# El client_id es intencionalmente público para clientes SPA (sin client_secret).
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# URI de redirección OAuth registrada en Google Cloud Console (BK-36).
# Debe coincidir EXACTAMENTE con la registrada; cualquier diferencia provoca error.
# Desarrollo local (Vite dev server): http://localhost:5173/
# GitHub Pages:                       https://nanomuelle.github.io/tensia/
# Google redirige aquí con ?code=...&state=... (query string, no hash).
# La app detecta ?code= en onMount de App.svelte antes de arrancar el router.
VITE_REDIRECT_URI=http://localhost:5173/

# ──────────────────────────────────────────────────────────
# Backend de desarrollo (solo entorno local y tests de integración)
# ──────────────────────────────────────────────────────────

# Puerto en el que escucha el servidor
PORT=3000

# Ruta al archivo JSON de persistencia (relativa a la raíz del proyecto)
DATA_FILE=data/measurements.json

# Sirve los ficheros estáticos del frontend desde el propio servidor backend.
# Activar en desarrollo/E2E (los scripts start y dev lo activan automáticamente).
SERVE_STATIC=true

# Abre el navegador automáticamente al arrancar el servidor.
# Activar en desarrollo (los scripts start y dev lo activan automáticamente).
OPEN_BROWSER=true
